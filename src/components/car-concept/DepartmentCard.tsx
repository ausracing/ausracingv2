// DepartmentCard.tsx
// OWNER: Yusuf
// Server component — individual card for each department (Chassis, Aero, Powertrain etc.)
// Props: title, summary, onClick
// Used inside: /car-concept page in a Preline card grid
export default function DepartmentCard() {
  return <div>DepartmentCard placeholder</div>
}
