// FundingBar.tsx
// OWNER: Yasmeen
// Server component — visual progress bar showing fundraising progress.
// Data: hardcoded for v1 (e.g. raised $15k / target $50k)
// Style: pure Tailwind, inner bar width = (raised / target) * 100%
export default function FundingBar() {
  return <section>FundingBar placeholder</section>
}
